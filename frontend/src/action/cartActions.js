import { CART_FAIL, CART_REQUEST, CART_SUCCESS } from "../constants/cartConstant"


export const addToCart = (pizza, quantity, varient) => (dispatch) => {
    dispatch({ type: CART_REQUEST })
    try {

    } catch (error) {

    }
}